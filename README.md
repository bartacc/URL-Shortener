# URL-Shortener
A simple URL Shortener web app in Django

## To run:

python3 -m venv venv

source venv/bin/activate

pip3 install -r requirements.txt

python3 manage.py migrate

python3 manage.py runserver
